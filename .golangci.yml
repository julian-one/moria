version: "2"

linters:
  enable:
    - gosec
  settings:
    errcheck:
      exclude-functions:
        - (*encoding/json.Encoder).Encode
    gosec:
      excludes:
        - G104
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
          - gosec
